name: networking-sm4ai1
topology:
  nodes:
    webserver:
      kind: linux
      image: networking-sm4ai1_webserver:latest
      ports:
        - "8080:80"

  links:
    - endpoints: ["webserver:eth1", "macvlan:wlp5s0"]
